# Icons

Placeholder. Generate icons before running `tauri build` with:

```sh
npm run tauri icon path/to/source-1024x1024.png
```

That populates this directory with the platform-specific formats listed in `tauri.conf.json` (icon.ico for Windows, icon.icns for macOS, the PNG variants for Linux).
