node_modules
dist
dist-ssr
*.local
.vite

# No committed lockfile — dependencies intentionally float to latest on every
# install/build (see package.json). Committing a lock would re-pin them.
package-lock.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Tauri build artefacts
src-tauri/target
src-tauri/gen
src-tauri/.fingerprint

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local agent workspace and locally-converted data dumps
.claude/
test-data/gswa-*/
